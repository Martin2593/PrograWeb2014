$(document).ready(
    function res(){

        var respuesta = document.getElementById('profundidad')
        var resultado = document.getElementById('subir') 

            if(profundidad.value<subir.value)
               error.innerHTML="valor no valido"
            else
                error.innerHTML="valor valido" 
                
            });
    }



);